# Exercise 1: JavaScript Basics & Setup

Just getting things set up here! 

I created an `index.html` file and linked an external script `main.js` at the bottom of the body. Inside `main.js`, I added a quick `console.log()` to print "Welcome to the Community Portal" to the DevTools console.

I also hooked up the `window.onload` event so an alert box pops up when the page is completely loaded. Simple and works perfectly.
