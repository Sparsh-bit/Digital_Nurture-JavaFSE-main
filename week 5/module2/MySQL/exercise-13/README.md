# Exercise 13: Average Rating per City

**Goal**: Calculate the average feedback rating of events conducted in each city.

Grouped by city, averaged the rating, and wrapped it in `ROUND(..., 2)` so it looks clean (like `4.25` instead of `4.250000`).
