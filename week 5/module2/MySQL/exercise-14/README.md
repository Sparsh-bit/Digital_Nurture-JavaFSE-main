# Exercise 14: Most Registered Events

**Goal**: List top 3 events based on the total number of user registrations.

Just a simple `GROUP BY` and `ORDER BY DESC` with a `LIMIT 3` applied to the registrations table.
