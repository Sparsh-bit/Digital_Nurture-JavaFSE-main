# Exercise 25: Events Without Sessions

**Goal**: List all events that currently have no sessions scheduled under them.

One last `LEFT JOIN` and `IS NULL` check to find events that don't have matching children in the Sessions table.
