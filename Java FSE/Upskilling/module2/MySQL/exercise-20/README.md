# Exercise 20: User Engagement Index

**Goal**: For each user, calculate how many events they attended and how many feedbacks they submitted.

Again, using `DISTINCT` on the counts to avoid Cartesian explosion since a user could have multiple registrations and multiple feedbacks across different events. 
