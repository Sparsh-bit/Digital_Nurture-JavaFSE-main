# Exercise 4: Leap Year Checker

Just a standard leap year check. I used the nested `if` logic to handle the divisible by 100 and 400 rule.
Compile with `javac LeapYear.java` and run with `java LeapYear`.
