# Exercise 14: Array Sum and Average

Populates an array dynamically based on user input, tracks the sum, and calculates the average at the end (making sure to cast to double so we don't lose the decimal).
