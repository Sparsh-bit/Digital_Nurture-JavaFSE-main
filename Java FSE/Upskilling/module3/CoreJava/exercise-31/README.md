# Exercise 31: Basic JDBC Connection

**Note:** To run this, you need a JDBC driver (like `sqlite-jdbc.jar`) in your classpath.
I used SQLite so we don't have to spin up a full MySQL server just to test a simple SELECT statement!
