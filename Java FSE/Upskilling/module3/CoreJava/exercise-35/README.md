# Exercise 35: TCP Client-Server Chat

**How to run:**
1. Open terminal 1 and compile/run `ChatServer.java` (it will hang and wait).
2. Open terminal 2 and run a TCP client (or use telnet/netcat: `nc localhost 5000`) and type messages!
