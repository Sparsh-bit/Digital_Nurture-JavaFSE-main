# Exercise 30: Pattern Matching for switch

**Note:** Requires JDK 21. 
Instead of doing `if (obj instanceof String) { String s = (String) obj; }`, the enhanced switch does the cast and assignment in one elegant line!
