# Exercise 41: Executor Service and Callable

Unlike `Runnable`, a `Callable` actually returns a value! We use the `Future.get()` method to retrieve the calculation result once the thread pool finishes processing it.
